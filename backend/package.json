{
  "name": "document-parser-api",
  "version": "1.0.0",
  "description": "API for parsing PDF and DOCX documents",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "postinstall": "npm --prefix ../frontend install && npm --prefix ../frontend run build && rm -rf public && mkdir -p public && cp -r ../frontend/dist/* public/ || true"
  },
  "keywords": ["pdf", "docx", "parser", "upload", "api"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "cors": "^2.8.5",
    "pdf-parse": "^1.1.1",
    "mammoth": "^1.6.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}